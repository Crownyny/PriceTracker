https://localhost:8443 {
    tls internal

    # Proxy all HTTP and SockJS/WebSocket traffic to the local backend
    reverse_proxy localhost:8080 {
        # Enable WebSocket upgrade
        header_up Connection "Upgrade"
        header_up Upgrade "websocket"
    }
}
